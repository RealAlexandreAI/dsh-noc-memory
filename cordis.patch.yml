# Plugin bundle for @alex/dsh-nocturne-memory.
# Registers the Nocturne Memory tools (nocturne_boot / read / search /
# create / update). Configure your own Nocturne MCP server:
#
#   - id: memory
#     name: '@alex/dsh-nocturne-memory'
#     config:
#       mcp_url: http://localhost:PORT/mcp
#       mcp_auth_ref: NOCTURNE_MCP_AUTH   # env var name (recommended)
- insert:
    - id: memory
      name: '@alex/dsh-nocturne-memory'
